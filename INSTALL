Linux
-----

type

sh build.sh

this will try to build the executables in the bin/ directory.

Windows
-------

I used Qt Creator, with the Microsoft Visual C++ 2008 Express tool chain.
Open the project file "cloudtrack.pro" build from there.


Prerequisits:
-------------

You need on Linux:
Qt4.6   (Core, Gui, Sql, Network)

You need on Windows:
Qt4.6  (Core, Gui, Sql, Network)
